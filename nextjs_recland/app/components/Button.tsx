interface ButtonProps{
    children: React.ReactNode
    variants: "primary" | "secondary" | "outlined" | "danger"
    classes: string
}
export const Button = ({children, variants, classes} : ButtonProps) => {
    return(
        <div className="">

        </div>
    )
}